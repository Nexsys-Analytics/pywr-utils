default_install_hook_types: [pre-commit, pre-push]

repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    # Ruff version.
    rev: v0.16.2
    hooks:
      # Auto-fix lint issues on staged files; --exit-zero means this never blocks a commit on its own -- CI's ruff check . (no --fix, no --exit-zero) is what actually enforces the ruleset, this hook is fix-and-warn, a convenience, not a gate.
      - id: ruff-check
        args: [--fix, --exit-zero]

  - repo: local
    hooks:
      - id: gitleaks-pre-push
        name: gitleaks (scan commits about to be pushed)
        language: system
        pass_filenames: false
        stages: [pre-push]
        entry: >-
          bash -c '
            UPSTREAM=$(git rev-parse --abbrev-ref --symbolic-full-name @{upstream} 2>/dev/null || echo "");
            if [ -n "$UPSTREAM" ]; then RANGE="${UPSTREAM}..HEAD"; else RANGE="-1 HEAD"; fi;
            echo "Scanning commit range: $RANGE";
            gitleaks detect --source . --log-opts="$RANGE" --config .gitleaks.toml --redact --verbose
          '
